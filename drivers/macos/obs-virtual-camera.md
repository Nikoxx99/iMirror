# OBS Virtual Camera on macOS

1. Install OBS Studio.
2. Add iMirror Desktop preview as a window/display source when V1 preview is running.
3. Start OBS Virtual Camera.
4. Select OBS Virtual Camera in Zoom, Teams, Meet, or another app.

This is a fallback workflow, not native iMirror camera output.
